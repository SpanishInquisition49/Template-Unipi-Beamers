\begin{frame}[fragile]{Code Listings}
  If you want to add code listings remember to add the \textbf{fragile} attribute on the frame
  \begin{lstlisting}[language=C++, frame=single, xleftmargin=4pt, xrightmargin=4pt]
int main(int argc, char **argv) {
  // a simple C program
  printf("Latex Rocks!\n");
  return EXIT_SUCCESS;
}
  \end{lstlisting}
\end{frame}
